<div class="search box">
  <h1>Search</h1>
<p>Search for people and posts</p>
<%= form_tag('/results', method: 'get') do %>
  <%= text_field_tag(:q) %><br>
  <%= select_tag :t, options_for_select([ "Tags", "People" ]) %>
  <%= submit_tag("Search") %>
<% end %>
</div>
